cef4delphi
tesseractocr
opencv


install cef4delphi "xx" Version
make sure use the binaries/ dll for the same "xx" version
copy the dlls to the bin/root project
also copy "locales" to the bin/root project


tesseractocr,make sure use the same dlls for the system you want to compile, ie: win32 or win64
there are different installers for those win32/win64
copy the dlls inside the DLL project
and the "tessdata" on the bin/root project


